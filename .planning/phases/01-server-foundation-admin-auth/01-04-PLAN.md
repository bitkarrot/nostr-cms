---
phase: 1
plan: 01-04
title: "useEmailEnabled() hook + email_enabled config flag (VITE_EMAIL_ENABLED override + swarm-config meta tag) + SPA gating of email admin nav"
wave: 1
depends_on: []
autonomous: true
requirements: [SRV-05]
threat_refs: []
files_modified:
  - src/lib/relay.ts
  - src/hooks/useEmailEnabled.ts
  - src/components/admin/AdminLayout.tsx
  - src/pages/admin/AdminEmailPage.tsx
  - src/AppRouter.tsx
  - src/hooks/useEmailEnabled.test.ts
  - src/components/admin/AdminLayout.test.tsx
---

# Plan 01-04 — useEmailEnabled() hook + email_enabled flag + SPA gating

<objective>
Add the `email_enabled` config flag and `useEmailEnabled()` hook that gates all email UI, mirroring the existing `getMasterPubkey()` priority (env wins over swarm-config meta tag, default off). Gate the admin sidebar email nav entry on the hook so no email nav renders when the module is off (SRV-05), and add a stub `/admin/email` route so the gated link does not 404 when enabled. This plan is SPA-only (no server files) and is independent of plans 01-01/01-02/01-03. Covers SRV-05.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-server-foundation-admin-auth/01-CONTEXT.md
@.planning/phases/01-server-foundation-admin-auth/01-RESEARCH.md
@.planning/phases/01-server-foundation-admin-auth/01-VALIDATION.md
@AGENTS.md
@src/lib/relay.ts
@src/lib/relay.test.ts
@src/components/admin/AdminLayout.tsx
@src/hooks/useRemoteNostrJson.ts
@src/AppRouter.tsx
</context>

<threat_model>
asvs_level: default
block_on: default
threats: []
notes: "No direct security threat in this plan — useEmailEnabled is a public UI-visibility flag (not a secret). The security-relevant guarantee is correctness of the default-off behavior (SRV-05): when email_enabled is unset everywhere, no email admin nav renders, so an installer who does not opt in sees no email surfaces. VITE_EMAIL_ENABLED is a legitimate public VITE_ var (controls UI only, no secrets), unlike the Resend key."
</threat_model>

<must_haves>
  truths:
    - "useEmailEnabled() returns true when VITE_EMAIL_ENABLED=true, false when =false"
    - "When VITE_EMAIL_ENABLED is unset and the swarm-config meta tag has email_enabled true, useEmailEnabled() returns true"
    - "When both env and meta are unset, useEmailEnabled() returns false (default off, SRV-05)"
    - "When both env and meta are set, env wins (consistency with getMasterPubkey)"
    - "The string \"true\" is coerced to boolean true; other strings coerce to false"
    - "AdminLayout renders no email nav entry when useEmailEnabled() is false"
    - "AdminLayout renders the email nav entry when useEmailEnabled() is true"
  artifacts:
    - path: "src/lib/relay.ts"
      provides: "email_enabled added to SwarmConfig interface + getEmailEnabled() pure function"
      contains: "getEmailEnabled"
    - path: "src/hooks/useEmailEnabled.ts"
      provides: "useEmailEnabled() hook wrapping getEmailEnabled()"
      contains: "useEmailEnabled"
    - path: "src/components/admin/AdminLayout.tsx"
      provides: "Email nav entry gated on useEmailEnabled() in the navigation array"
      contains: "useEmailEnabled"
    - path: "src/pages/admin/AdminEmailPage.tsx"
      provides: "Stub admin email page (coming-soon placeholder) so the gated nav link does not 404"
      contains: "Email"
    - path: "src/AppRouter.tsx"
      provides: "/admin/email route registered to AdminEmailPage"
      contains: "admin/email"
  key_links:
    - from: "src/components/admin/AdminLayout.tsx"
      to: "src/hooks/useEmailEnabled.ts"
      via: "conditional spread of the email nav entry based on useEmailEnabled()"
      pattern: "useEmailEnabled"
    - from: "src/hooks/useEmailEnabled.ts"
      to: "src/lib/relay.ts"
      via: "calls getEmailEnabled() (env-over-meta priority, default off)"
      pattern: "getEmailEnabled"
  prohibitions:
    - "No server-only dep imported from src/ (the ESLint guard from 01-03 enforces this)"
    - "No email nav rendered when email_enabled is false (SRV-05)"
    - "No secret exposed via VITE_EMAIL_ENABLED (it is a public UI flag only)"
    - "No swarm repo modification to add the meta tag (swarm config template is a separate-repo follow-up per AGENTS.md)"
</must_haves>

## Artifacts this phase produces

- `src/lib/relay.ts` (edit) — `email_enabled?: boolean` added to the `SwarmConfig` interface (lines 12-15) and a new exported `getEmailEnabled(): boolean` function (env `VITE_EMAIL_ENABLED` wins over `getSwarmConfig().email_enabled`, default `false`).
- `src/hooks/useEmailEnabled.ts` — `useEmailEnabled(): boolean` thin hook returning `getEmailEnabled()`.
- `src/components/admin/AdminLayout.tsx` (edit) — imports `useEmailEnabled` and `Mail` from lucide-react; adds a conditional email nav entry (`...(emailEnabled ? [{ name: 'Email', href: '/admin/email', icon: Mail }] : [])`) in the `navigation` array.
- `src/pages/admin/AdminEmailPage.tsx` — stub "coming soon" page so the gated nav link resolves.
- `src/AppRouter.tsx` (edit) — registers the `/admin/email` route to `AdminEmailPage`.
- `src/hooks/useEmailEnabled.test.ts`, `src/components/admin/AdminLayout.test.tsx` — filled from the 01-01-00 stubs.

<tasks>

<task id="01-04-01" type="auto" wave="1">
  <objective>getEmailEnabled() + useEmailEnabled() hook — env/meta resolution, default off (SRV-05).</objective>
  <files>src/lib/relay.ts, src/hooks/useEmailEnabled.ts, src/hooks/useEmailEnabled.test.ts</files>
  <read_first>
    - `src/lib/relay.ts` (the `SwarmConfig` interface lines 12-15; `getSwarmConfig()` lines 20-32; `getMasterPubkey()` lines 38-57 — the env-over-meta priority pattern to mirror).
    - `src/hooks/useEmailEnabled.test.ts` (stub from 01-01-00).
    - `src/lib/relay.test.ts` (existing vitest pattern in jsdom).
    - RESEARCH.md §6.1 (mirror getMasterPubkey exactly — env wins over meta; default false; string "true" coercion; VITE_EMAIL_ENABLED is a legitimate public VITE_ var), §7.3 edge cases (env+meta both set → env wins; absent everywhere → false).
    - CONTEXT.md "Claude's Discretion" (useEmailEnabled priority — env wins over meta tag, default off when neither set).
  </read_first>
  <action>
    Edit `src/lib/relay.ts`: add `email_enabled?: boolean;` to the `SwarmConfig` interface (alongside `masterPubkey?` and `relayName?`). Add a new exported function `getEmailEnabled(): boolean` mirroring `getMasterPubkey`'s priority: read `import.meta.env.VITE_EMAIL_ENABLED` first; if it is not `undefined`, return `String(envEnabled).toLowerCase() === 'true'` (string coercion — only the literal true string yields true). Else read `getSwarmConfig().email_enabled`; if it is not `undefined`, return `!!injected`. Else return `false` (default off, SRV-05). Do NOT use truthy coercion on the env value — explicitly compare the lowercased string to `'true'` so `"false"` and arbitrary strings yield false (RESEARCH §6.1). `VITE_EMAIL_ENABLED` is a public UI flag (not a secret), so it is a legitimate VITE_ var.

    Create `src/hooks/useEmailEnabled.ts` exporting `useEmailEnabled(): boolean` that returns `getEmailEnabled()` (thin hook, no TanStack Query — the value is read synchronously from env/meta at render, like `getMasterPubkey` is used in `useRemoteNostrJson.ts` line 31; a page reload picks up meta-tag changes, RESEARCH §6.1).

    Fill `src/hooks/useEmailEnabled.test.ts` (jsdom env, no Node pragma — this is a SPA test) cases: (1) `VITE_EMAIL_ENABLED=true` → true; (2) `=false` → false; (3) unset + meta tag `email_enabled: true` → true; (4) unset + meta absent → false (default off); (5) env=`"true"` string coercion → true; (6) env=`"false"` string → false. Set `import.meta.env.VITE_EMAIL_ENABLED` via `vi.stubEnv` (or `vi.resetModules` + env manipulation) and the meta tag via `document.head.innerHTML = '<meta name="swarm-config" content=\'{"email_enabled":true}\'>"` in jsdom. Reset env + meta between tests in afterEach.
  </action>
  <acceptance_criteria>
    - `getEmailEnabled()` returns true only when `VITE_EMAIL_ENABLED` is the string "true" (case-insensitive) or, with env unset, the meta tag `email_enabled` is true.
    - Default (neither set) → false (SRV-05).
    - `useEmailEnabled()` returns `getEmailEnabled()`.
    - `npx vitest run src/hooks/useEmailEnabled.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/hooks/useEmailEnabled.test.ts</automated>
  </verify>
</task>

<task id="01-04-02" type="auto" wave="1">
  <objective>Priority test — env wins over meta when both set (consistency with getMasterPubkey); string coercion.</objective>
  <files>src/hooks/useEmailEnabled.test.ts</files>
  <read_first>
    - `src/hooks/useEmailEnabled.test.ts` (the 01-04-01 cases already in place).
    - `src/lib/relay.ts` `getMasterPubkey()` (the env-over-meta priority this must match).
    - RESEARCH.md §7.3 (env + meta both set → env wins; the reverse VITE=false, meta=true → false).
  </read_first>
  <action>
    Add priority cases to `src/hooks/useEmailEnabled.test.ts`: (1) `VITE_EMAIL_ENABLED=true` + meta `email_enabled: false` → true (env wins); (2) `VITE_EMAIL_ENABLED=false` + meta `email_enabled: true` → false (env wins, the reverse case); (3) env set to a non-`"true"` string (e.g. `"1"`) + meta `true` → false (string coercion: only `"true"` is true, so env's `"1"` resolves false and, because env is set, meta is NOT consulted — assert false, proving env-wins short-circuits before meta). These lock the consistency guarantee with `getMasterPubkey` (CONTEXT.md "Claude's Discretion": env wins over meta tag). Use the same env/meta setup helpers from 01-04-01.
  </action>
  <acceptance_criteria>
    - env=true + meta=false → true (env wins).
    - env=false + meta=true → false (env wins).
    - env set to non-"true" string + meta=true → false (env short-circuits, string coercion).
    - `npx vitest run src/hooks/useEmailEnabled.test.ts` is green (all 01-04-01 + 01-04-02 cases).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/hooks/useEmailEnabled.test.ts</automated>
  </verify>
</task>

<task id="01-04-03" type="auto" wave="1">
  <objective>Gate the AdminLayout email nav entry on useEmailEnabled() + stub /admin/email route (no 404 when enabled, no nav when disabled).</objective>
  <files>src/components/admin/AdminLayout.tsx, src/pages/admin/AdminEmailPage.tsx, src/AppRouter.tsx, src/components/admin/AdminLayout.test.tsx</files>
  <read_first>
    - `src/components/admin/AdminLayout.tsx` (the `navigation` array lines 94-114 — note the conditional spread pattern for `isSchedulerHealthy` line 98 and `canAccessSettings` lines 108-111; the lucide-react import block lines 16-37; the component body lines 39-53).
    - `src/components/admin/AdminLayout.test.tsx` (stub from 01-01-00).
    - `src/AppRouter.tsx` (the route registration pattern for admin pages).
    - RESEARCH.md §6.2 (gate the admin nav entry — add useEmailEnabled import + Mail icon + conditional spread; the validation requires the nav gating in Phase 1, so add the nav entry + a stub route rather than deferring).
    - VALIDATION.md task 01-04-03 row (source assertion: grep useEmailEnabled in AdminLayout + vitest run AdminLayout.test.tsx).
  </read_first>
  <action>
    Edit `src/components/admin/AdminLayout.tsx`: add `Mail` to the `lucide-react` import block (lines 16-37); add `import { useEmailEnabled } from '@/hooks/useEmailEnabled';`; call `const emailEnabled = useEmailEnabled();` in the component body (near the other hook calls lines 50-53); add a conditional spread entry to the `navigation` array — `...(emailEnabled ? [{ name: 'Email', href: '/admin/email', icon: Mail }] : [])` — placed after the Scheduled entry (line 98) or near the Settings group. When `emailEnabled` is false, no email nav item is added (SRV-05: "renders no email admin nav item"). Match the existing conditional-spread style exactly.

    Create `src/pages/admin/AdminEmailPage.tsx` — a minimal stub page (per AGENTS.md admin page convention: `Admin[Feature].tsx` + thin wrapper page). Render a simple "Email — coming soon" placeholder inside the admin layout shell so the gated nav link resolves instead of 404ing. This is a placeholder page, not the Phase 2 settings UI — keep it minimal (a heading + short text). The real AdminEmail settings UI lands in Phase 2.

    Edit `src/AppRouter.tsx` to register the `/admin/email` route pointing to `AdminEmailPage` (wrapped in `AdminLayout` if the router nests admin routes under a layout route — follow the existing pattern for other admin pages). This ensures the nav entry, when visible, links to a real route.

    Fill `src/components/admin/AdminLayout.test.tsx` (jsdom) cases: (1) when `useEmailEnabled` returns false, the rendered navigation does NOT contain an item named "Email" (assert no link with href `/admin/email`); (2) when `useEmailEnabled` returns true, the navigation DOES contain the "Email" item with href `/admin/email`. Mock `useEmailEnabled` via `vi.mock('@/hooks/useEmailEnabled', () => ({ useEmailEnabled: () => false }))` for case 1 and `() => true` for case 2 (or use `vi.mocked`). Render `AdminLayout` with the required providers/router context (follow the existing test setup in `src/test/setup.ts` and any provider wrapping the component needs — `MemoryRouter` + `AppProvider`/`NostrProvider` as required; keep the test minimal by mocking heavy dependencies like `useAdminAuth`/`useSchedulerHealth` to avoid relay/network calls). Assert via querying the rendered nav links.
  </action>
  <acceptance_criteria>
    - `AdminLayout.tsx` imports and calls `useEmailEnabled()` and conditionally spreads the email nav entry.
    - When `useEmailEnabled()` is false, no "Email" nav item renders (SRV-05).
    - When `useEmailEnabled()` is true, the "Email" nav item with href `/admin/email` renders.
    - `/admin/email` route is registered in `AppRouter.tsx` to `AdminEmailPage` (no 404 when enabled).
    - `grep -c 'useEmailEnabled' src/components/admin/AdminLayout.tsx` returns >= 1.
    - `npx vitest run src/components/admin/AdminLayout.test.tsx` is green.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'useEmailEnabled' src/components/admin/AdminLayout.tsx && npx vitest run src/components/admin/AdminLayout.test.tsx</automated>
  </verify>
</task>

</tasks>
