---
phase: 1
plan: 01-03
title: "nginx location block snippet + npm run server entry + ESLint guard forbidding server/-only imports under src/"
wave: 1
depends_on: [01-01]
autonomous: true
requirements: [SRV-01, SRV-04]
threat_refs: [T-01-04]
files_modified:
  - server/deploy/nginx.example.conf
  - server/deploy/nostr-cms-email.service
  - server/deploy/email.env
  - server/deploy/README.md
  - eslint.config.js
  - package.json
  - eslint-rules/__tests__/guard.test.ts
---

# Plan 01-03 — nginx snippet + npm run server entry + ESLint guard

<objective>
Provide the hardened nginx `location /api/email/` snippet (D-02 in nostr-cms repo, D-03 hardened with `client_max_body_size 10m` + `proxy_read_timeout 60s` + forwarded headers, no nginx rate limiting, no WebSocket upgrade headers), the systemd unit + env template for the long-running process (SRV-04), and the ESLint `no-restricted-imports` guard forbidding `resend`/`better-sqlite3`/`csv-parse`/`pg` and `server/*` imports under `src/` (SRV-01, T-01-04). Also wire `server:check` into the main `test` script so server type errors fail CI. Covers SRV-01 (eslint guard) and SRV-04 (nginx + long-running process). swarm is not modified (D-02).
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-server-foundation-admin-auth/01-CONTEXT.md
@.planning/phases/01-server-foundation-admin-auth/01-RESEARCH.md
@.planning/phases/01-server-foundation-admin-auth/01-VALIDATION.md
@.planning/phases/01-server-foundation-admin-auth/01-01-PLAN.md
@AGENTS.md
@eslint.config.js
@eslint-rules/index.js
@package.json
</context>

<threat_model>
asvs_level: default
block_on: default
threats:
  - id: T-01-04
    description: "Server-only import leak into the client bundle — a src/ file imports resend/better-sqlite3/csv-parse/pg or reaches into server/, bundling secrets or Node-only code into the public SPA."
    mitigation: "ESLint no-restricted-imports rule scoped to src/** flags the four server-only packages and server/* path patterns as errors; a Linter-based test asserts the rule fires on a fixture and does not fire on a server/ file. CI runs eslint as part of npm run test."
    verification: "eslint-rules/__tests__/guard.test.ts — task 01-03-01."
</threat_model>

<must_haves>
  truths:
    - "A src/ file importing resend, better-sqlite3, csv-parse, or pg produces an ESLint error"
    - "A src/ file importing from server/ (any relative path) produces an ESLint error"
    - "A server/ file importing better-sqlite3 (or the others) produces NO error"
    - "server/deploy/nginx.example.conf exists with client_max_body_size 10m, proxy_read_timeout 60s, and an EMAIL_PORT placeholder"
    - "npm run server starts the Node process (package.json scripts.server is defined)"
    - "server:check is wired into the main test script so server type errors fail CI"
  artifacts:
    - path: "server/deploy/nginx.example.conf"
      provides: "Hardened nginx location /api/email/ snippet (D-02, D-03) with EMAIL_PORT placeholder"
      contains: "client_max_body_size 10m"
    - path: "server/deploy/nostr-cms-email.service"
      provides: "systemd unit template for the long-running email service (SRV-04)"
      contains: "Restart=on-failure"
    - path: "server/deploy/email.env"
      provides: "Env var template (EMAIL_PORT, EMAIL_DB_PATH, EMAIL_DB_BACKEND, MASTER_PUBKEY, SWARM_BASE_URL)"
      contains: "EMAIL_PORT"
    - path: "server/deploy/README.md"
      provides: "Deploy instructions: copy nginx snippet, install systemd unit, start service"
      contains: "nginx"
    - path: "eslint.config.js"
      provides: "no-restricted-imports block scoped to src/** forbidding server-only packages + server/* paths"
      contains: "no-restricted-imports"
  key_links:
    - from: "eslint.config.js"
      to: "src/**"
      via: "no-restricted-imports rule fires on src/ files importing server-only deps or server/*"
      pattern: "no-restricted-imports"
    - from: "server/deploy/nginx.example.conf"
      to: "server/index.ts"
      via: "proxy_pass http://127.0.0.1:${EMAIL_PORT} routes /api/email/* to the Node process started by npm run server"
      pattern: "proxy_pass"
  prohibitions:
    - "No modification to the swarm repo (D-02 — nginx snippet lives in nostr-cms/server/deploy/)"
    - "No nginx-level rate limiting (D-03 — rate limiting is server-side token bucket, Phase 5)"
    - "No WebSocket upgrade headers in the nginx snippet (D-03 — email API is HTTP-only)"
    - "No server-only package importable from src/ (the guard must fire)"
</must_haves>

## Artifacts this phase produces

- `server/deploy/nginx.example.conf` — the `location /api/email/ { ... }` block (D-02/D-03) with `proxy_pass http://127.0.0.1:${EMAIL_PORT}`, `proxy_http_version 1.1`, `proxy_set_header Host/X-Real-IP/X-Forwarded-For/X-Forwarded-Proto/X-Forwarded-Host`, `client_max_body_size 10m`, `proxy_read_timeout 60s`; no rate limiting, no upgrade headers.
- `server/deploy/nostr-cms-email.service` — systemd unit (Type=simple, WorkingDirectory, EnvironmentFile=email.env, ExecStart=`npx tsx server/index.ts`, Restart=on-failure, RestartSec=5).
- `server/deploy/email.env` — template env file (EMAIL_PORT=3001, EMAIL_DB_PATH, EMAIL_DB_BACKEND=sqlite, MASTER_PUBKEY, SWARM_BASE_URL).
- `server/deploy/README.md` — copy-paste nginx install steps + systemd enable steps + the manual-verify pointers from VALIDATION.md.
- `eslint.config.js` (edit) — new `files: ["src/**/*.{ts,tsx}"]` config block with `no-restricted-imports` error rule (paths: resend, better-sqlite3, csv-parse, pg; patterns: server/* relative imports).
- `package.json` (edit) — wire `server:check` into the `test` script so server type errors fail CI.
- `eslint-rules/__tests__/guard.test.ts` — Linter-based test asserting the guard fires on src/ fixtures and not on server/ fixtures (filled from the 01-01-00 stub).

<tasks>

<task id="01-03-01" type="auto" wave="1">
  <objective>ESLint no-restricted-imports guard forbidding server-only packages + server/* paths under src/ (SRV-01, T-01-04).</objective>
  <files>eslint.config.js, eslint-rules/__tests__/guard.test.ts, package.json</files>
  <read_first>
    - `eslint.config.js` (flat config; the `**/*.{ts,tsx}` block lines 13-47 applies browser globals; add a new src/** block after it).
    - `eslint-rules/index.js` (existing custom rules infra — not needed for this task since no-restricted-imports is built-in, but confirms the plugin pattern).
    - `eslint-rules/__tests__/guard.test.ts` (stub from 01-01-00).
    - RESEARCH.md §5.1 (no-restricted-imports for both package names and server/* path patterns; scoped to src/**; server files get their own lint pass), §5 (why not eslint-plugin-import / custom rule).
    - `package.json` lines 6-11 (the `test` script — wire server:check in).
  </read_first>
  <action>
    Edit `eslint.config.js` to add a new config block inside `tseslint.config(...)` after the existing `**/*.{ts,tsx}` block. The new block: `files: ["src/**/*.{ts,tsx}"]`, `rules: { "no-restricted-imports": ["error", { paths: [{ name: "resend", message: "resend is server-only. Import it from server/ only." }, { name: "better-sqlite3", message: "better-sqlite3 is server-only. Import it from server/ only." }, { name: "csv-parse", message: "csv-parse is server-only. Import it from server/ only." }, { name: "pg", message: "pg is server-only. Import it from server/ only." } ], patterns: [{ group: ["server/*", "../server/*", "../../server/*", "../../../server/*"], message: "Importing from server/ is forbidden in src/ (server-only boundary)." }] }] }`. This scopes the guard to src/ only — server/ files legitimately import better-sqlite3 etc. and are not affected (RESEARCH §5.1). Do NOT add a separate server/** lint block in this task (server linting runs via `server:check` + eslint on server/; the existing `**/*.{ts,tsx}` block already lints server/ with browser globals — acceptable for Phase 1, or add a server/** block with `globals.node` if eslint errors on server/ Node globals; use judgment).

    Fill `eslint-rules/__tests__/guard.test.ts` using ESLint's `Linter` (from `eslint`) to verify the guard config. The test: instantiate a `Linter`, define `no-restricted-imports` with the same config as eslint.config.js, and lint fixture source strings. Assert errors are reported for: a src/ fixture importing `resend`; a src/ fixture importing `better-sqlite3`; a src/ fixture importing `csv-parse`; a src/ fixture importing `pg`; a src/ fixture importing from `server/db/sqlite` (relative path pattern). Assert NO errors for: a server/ fixture importing `better-sqlite3` (the rule is scoped to src/**, so when the fixture is treated as a server/ file the rule does not apply — model this by NOT enabling the rule for the server/ fixture, or by using the `files` match semantics; simplest: run the rule only on src/ fixtures and assert server/ fixtures produce no `no-restricted-imports` errors). This is the VALIDATION RuleTester alternative (VALIDATION task 01-03-01 offers `(or RuleTester)`).

    Edit `package.json` `test` script to include `server:check` so server type errors fail CI — append `&& npm run server:check` to the existing `test` script (after the build steps, or before vitest; place it logically after `tsc --noEmit` and before or after `eslint`). Keep the existing pipeline intact; only append the server check.
  </action>
  <acceptance_criteria>
    - `eslint.config.js` has a `src/**/*.{ts,tsx}` block with `no-restricted-imports` error covering resend, better-sqlite3, csv-parse, pg (paths) and server/* relative imports (patterns).
    - The guard test asserts errors for each forbidden src/ import and no error for a server/ file importing better-sqlite3.
    - `package.json` `test` script runs `server:check`.
    - `npx vitest run eslint-rules/__tests__/guard.test.ts` is green.
    - `npx eslint` (full run) exits 0 on the current codebase (no new false positives in src/).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run eslint-rules/__tests__/guard.test.ts</automated>
  </verify>
</task>

<task id="01-03-02" type="auto" wave="1">
  <objective>nginx snippet (D-02/D-03) + systemd unit + env template + npm run server entry verification (SRV-04).</objective>
  <files>server/deploy/nginx.example.conf, server/deploy/nostr-cms-email.service, server/deploy/email.env, server/deploy/README.md, package.json</files>
  <read_first>
    - `package.json` (scripts — confirm `server` script exists from 01-01-00; this task verifies it).
    - CONTEXT.md D-01 (EMAIL_PORT default 3001), D-02 (nginx snippet in server/deploy/ in nostr-cms repo, not swarm; ${EMAIL_PORT} placeholder), D-03 (hardened: client_max_body_size 10m, proxy_read_timeout 60s, forwarded headers; no nginx rate limiting; no WebSocket upgrade headers).
    - RESEARCH.md §4.1 (nginx snippet exact content + routing logic — longest-prefix match means /api/email/ wins over /api/), §4.2 (systemd unit template + email.env contents).
    - VALIDATION.md task 01-03-02 row (source assertion: grep client_max_body_size 10m + node -e require package.json scripts.server) and Manual-Only Verifications (nginx proxy + systemd restart — manual, not automated here).
  </read_first>
  <action>
    Create `server/deploy/nginx.example.conf` containing the `location /api/email/ { ... }` block per D-02/D-03 and RESEARCH §4.1: a header comment explaining it is copy-pasted into the existing nginx server block with EMAIL_PORT substituted; `proxy_pass http://127.0.0.1:${EMAIL_PORT};`, `proxy_http_version 1.1;`, `proxy_set_header Host $host;`, `proxy_set_header X-Real-IP $remote_addr;`, `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`, `proxy_set_header X-Forwarded-Proto $scheme;`, `proxy_set_header X-Forwarded-Host $host;`, `client_max_body_size 10m;` (covers future Phase 4 CSV uploads), `proxy_read_timeout 60s;` (covers slow admin operations). Do NOT include any rate-limiting directive (D-03: rate limiting is server-side, Phase 5). Do NOT include `proxy_set_header Upgrade`/`Connection "upgrade"` (D-03: email API is HTTP-only). The X-Forwarded-Host + X-Forwarded-Proto headers are required so the URL reconstruction in plan 01-02 (T-01-03) works.

    Create `server/deploy/nostr-cms-email.service` — a systemd unit template (RESEARCH §4.2): `[Unit] Description=nostr-cms email service / After=network.target`; `[Service] Type=simple / WorkingDirectory=/app/nostr-cms / EnvironmentFile=/app/nostr-cms/server/deploy/email.env / ExecStart=/usr/bin/npx tsx server/index.ts / Restart=on-failure / RestartSec=5 / User=nostr-cms`; `[Install] WantedBy=multi-user.target`. This is the recommended process supervisor (CONTEXT.md "Claude's Discretion" — systemd chosen as the lowest-friction option for a relay box already running swarm; manual-verify per VALIDATION).

    Create `server/deploy/email.env` — a template env file with commented values: `EMAIL_PORT=3001`, `EMAIL_DB_PATH=/app/email.db`, `EMAIL_DB_BACKEND=sqlite`, `EMAIL_BACKUP_DIR=/app/backups`, `MASTER_PUBKEY=`, `SWARM_BASE_URL=https://relay.example.com`. No secrets in the template (RESEND_API_KEY is Phase 2). Mark it clearly as a template the installer edits.

    Create `server/deploy/README.md` with: (1) nginx steps — copy the `location /api/email/` snippet into the existing server block, substitute EMAIL_PORT, `sudo nginx -t && sudo systemctl reload nginx`; (2) systemd steps — copy the unit to `/etc/systemd/system/`, edit paths/env, `sudo systemctl daemon-reload && sudo systemctl enable --now nostr-cms-email`; (3) note that swarm is NOT modified (D-02); (4) pointer to the manual verifications in VALIDATION.md (nginx proxy curl + systemd crash-restart). Confirm `package.json` has a `server` script (`tsx server/index.ts`) from 01-01-00 — if not present, add it (defensive; it should already exist).
  </action>
  <acceptance_criteria>
    - `server/deploy/nginx.example.conf` exists and contains `client_max_body_size 10m` and `proxy_read_timeout 60s` and the `${EMAIL_PORT}` placeholder and `X-Forwarded-Host`/`X-Forwarded-Proto` headers.
    - The nginx snippet contains NO rate-limiting directive and NO WebSocket upgrade headers.
    - `server/deploy/nostr-cms-email.service` exists with `Restart=on-failure` and `ExecStart` invoking `tsx server/index.ts`.
    - `server/deploy/email.env` exists with the env var template (no secrets).
    - `package.json` scripts include `server`.
    - `grep -c 'client_max_body_size 10m' server/deploy/nginx.example.conf` returns >= 1 and `node -e "require('./package.json').scripts.server"` prints the server script.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'client_max_body_size 10m' server/deploy/nginx.example.conf && node -e "require('./package.json').scripts.server"</automated>
  </verify>
</task>

</tasks>
