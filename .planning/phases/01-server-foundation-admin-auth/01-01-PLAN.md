---
phase: 1
plan: 01-01
title: "server/ scaffold + SubscriberRepository interface + SQLite implementation + migrations (WAL mode, online backup)"
wave: 1
depends_on: []
autonomous: true
requirements: [SRV-02, SRV-01]
threat_refs: [T-01-01]
files_modified:
  - package.json
  - server/tsconfig.json
  - server/index.ts
  - server/routes/health.ts
  - server/db/repository.ts
  - server/db/sqlite.ts
  - server/db/migrate.ts
  - server/db/migrations/001.sqlite.sql
  - server/db/backup.ts
  - server/README.md
  - server/db/sqlite.repository.test.ts
  - server/db/migrations.test.ts
  - server/db/sqlite.test.ts
  - server/db/backup.test.ts
  - server/routes/health.test.ts
  - server/auth/nip98.test.ts
  - server/auth/master-pubkey.test.ts
  - src/hooks/useEmailEnabled.test.ts
  - src/components/admin/AdminLayout.test.tsx
  - eslint-rules/__tests__/guard.test.ts
---

# Plan 01-01 — server/ scaffold + SubscriberRepository + SQLite + migrations

<objective>
Stand up the Node/TS email service scaffold in `server/`, the backend-agnostic `SubscriberRepository` interface, its SQLite implementation (`better-sqlite3`, WAL mode), a tiny idempotent SQL migration runner with the initial schema (subscribers, settings, verify_tokens, send_log, delivery_events — all carrying `site_id`), and the online-backup capability. Also perform the Wave 0 setup for the entire phase: install `hono`, `@hono/node-server`, `better-sqlite3`, `tsx`; add a server `tsconfig.json`; add `@vitest-environment node` pragmas + stub every test file listed in VALIDATION.md Wave 0 Requirements. Covers SRV-02 (repository + SQLite + migrations + WAL + online backup) and the SRV-01 server scaffold root. Implements D-01 (EMAIL_PORT default 3001) in the entry point.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-server-foundation-admin-auth/01-CONTEXT.md
@.planning/phases/01-server-foundation-admin-auth/01-RESEARCH.md
@.planning/phases/01-server-foundation-admin-auth/01-VALIDATION.md
@AGENTS.md
@src/lib/relay.ts
@src/lib/relay.test.ts
@vite.config.ts
@tsconfig.json
@package.json
</context>

<threat_model>
asvs_level: default
block_on: default
threats:
  - id: T-01-01
    description: "Cross-site repository reads/writes — a query scoped to one site_id returns or mutates another site's rows."
    mitigation: "Every SubscriberRepository method takes siteId as its first argument and every SQL query includes a WHERE site_id = ? predicate keyed on it; the integration test asserts a row inserted under site_id 'A' is invisible to a lookup under site_id 'B'."
    verification: "server/db/sqlite.repository.test.ts — cross-site isolation case (task 01-01-01)."
</threat_model>

<must_haves>
  truths:
    - "A SubscriberRepository interface exists with methods scoped by site_id and a SQLite implementation backed by better-sqlite3"
    - "WAL mode is enabled after opening the database (PRAGMA journal_mode returns wal)"
    - "The migration runner is idempotent — re-running applies no new files and schema_migrations records each applied version once"
    - "An online backup produces a valid, openable SQLite file with the same row count, even mid-write"
    - "The server entry reads EMAIL_PORT (default 3001 per D-01) and starts a Hono app on @hono/node-server"
    - "GET /api/email/health returns {\"ok\":true} with no auth and no DB details (D-04)"
    - "Server-only deps are installed and server test files run in the Node vitest environment"
  artifacts:
    - path: "server/db/repository.ts"
      provides: "SubscriberRepository interface + Subscriber/Settings/VerifyToken/SendLog/DeliveryEvent row types"
      contains: "export interface SubscriberRepository"
    - path: "server/db/sqlite.ts"
      provides: "SqliteSubscriberRepository implementing SubscriberRepository with WAL + busy_timeout"
      contains: "journal_mode"
    - path: "server/db/migrate.ts"
      provides: "Idempotent migration runner recording schema_migrations"
      contains: "schema_migrations"
    - path: "server/db/migrations/001.sqlite.sql"
      provides: "Initial schema for subscribers, settings, verify_tokens, send_log, delivery_events (all with site_id)"
      contains: "CREATE TABLE subscribers"
    - path: "server/db/backup.ts"
      provides: "Online backup via better-sqlite3 backup API + runBackup entry"
      contains: "backup"
    - path: "server/index.ts"
      provides: "Server entry: Hono app, run migrations, register health route, serve on EMAIL_PORT"
      contains: "EMAIL_PORT"
    - path: "server/routes/health.ts"
      provides: "Public health route returning {\"ok\":true}"
      contains: "ok"
    - path: "server/tsconfig.json"
      provides: "Server-only TS config with Node types, no DOM lib"
      contains: "ESNext"
  key_links:
    - from: "server/index.ts"
      to: "server/db/sqlite.ts"
      via: "instantiates SqliteSubscriberRepository based on EMAIL_DB_BACKEND env (default sqlite)"
      pattern: "SqliteSubscriberRepository"
    - from: "server/index.ts"
      to: "server/db/migrate.ts"
      via: "runs migrations before serve()"
      pattern: "runMigrations"
    - from: "server/index.ts"
      to: "server/routes/health.ts"
      via: "registers the public health route"
      pattern: "health"
  prohibitions:
    - "No server-only dep (resend, better-sqlite3, csv-parse, pg) imported from src/"
    - "No subscriber PII written to relays"
    - "No DB details or subscriber counts in the health response body"
    - "No DOM lib in server/tsconfig.json (server code must not use window/document)"
</must_haves>

## Artifacts this phase produces

- `server/tsconfig.json` — server TS config (extends no DOM; types: ["node"]; lib: ["ESNext"]; include: ["server"]).
- `server/index.ts` — entry: builds Hono app, selects DB backend via `EMAIL_DB_BACKEND` (default `sqlite`), opens `SqliteSubscriberRepository` at `EMAIL_DB_PATH` (default `./email.db`), runs migrations, registers the public health route, and `serve()`s on `EMAIL_PORT` (default `3001`, per D-01). Exports a `createApp(repo?)` helper for test mounting (no port binding on import).
- `server/routes/health.ts` — exports a Hono handler returning `c.json({ ok: true }, 200)` (D-04: no auth, no DB details).
- `server/db/repository.ts` — `Subscriber`, `Settings`, `VerifyToken`, `SendLog`, `DeliveryEvent` types + `SubscriberRepository` interface (all methods `async`, scoped by `siteId`).
- `server/db/sqlite.ts` — `SqliteSubscriberRepository implements SubscriberRepository` (WAL + `foreign_keys=ON` + `busy_timeout=5000` on open).
- `server/db/migrate.ts` — `runMigrations(db, dir)` idempotent runner; records `schema_migrations(version, applied_at)`.
- `server/db/migrations/001.sqlite.sql` — initial schema (5 tables + `schema_migrations`), all carrying `site_id`, `UNIQUE(site_id, email)` on subscribers.
- `server/db/backup.ts` — `backupDatabase(db, destPath)` + `runBackup()` (daily default; `EMAIL_BACKUP_DIR` default `/app/backups`, 7-day retention).
- `server/README.md` — dev/prod run instructions, env vars, backup cron line.
- `package.json` — new scripts `server`, `server:dev`, `server:check`, `server:backup`; new deps `hono`, `@hono/node-server`, `better-sqlite3`; devDep `tsx`.
- Test files (stubs created in Wave 0, filled by this + later plans): `server/db/sqlite.repository.test.ts`, `server/db/migrations.test.ts`, `server/db/sqlite.test.ts`, `server/db/backup.test.ts`, `server/routes/health.test.ts`, `server/auth/nip98.test.ts`, `server/auth/master-pubkey.test.ts`, `src/hooks/useEmailEnabled.test.ts`, `src/components/admin/AdminLayout.test.tsx`, `eslint-rules/__tests__/guard.test.ts`.

<tasks>

<task id="01-01-00" type="auto" wave="0">
  <objective>Wave 0 setup for the entire phase: install server deps, add server tsconfig + npm scripts, stub every test file, add Node-environment pragmas.</objective>
  <files>package.json, server/tsconfig.json, server/db/sqlite.repository.test.ts, server/db/migrations.test.ts, server/db/sqlite.test.ts, server/db/backup.test.ts, server/routes/health.test.ts, server/auth/nip98.test.ts, server/auth/master-pubkey.test.ts, src/hooks/useEmailEnabled.test.ts, src/components/admin/AdminLayout.test.tsx, eslint-rules/__tests__/guard.test.ts</files>
  <read_first>
    - `package.json` (scripts block lines 6-11; dependencies lines 12-85; devDependencies lines 94-119 — note `@nostrify/nostrify` ^0.48.2 line 20, `nostr-tools` ^2.13.0 line 68, `vitest` ^4.1.0 line 118, `@types/node` line 101 already present).
    - `vite.config.ts` (test config lines 29-39: jsdom env, setupFiles `./src/test/setup.ts`).
    - `tsconfig.json` (include: ["src"] line 30; lib includes DOM line 5).
    - `src/lib/relay.test.ts` (existing vitest pattern: `import { describe, expect, it } from 'vitest'`).
    - VALIDATION.md "Wave 0 Requirements" (lines 62-79) — the exact stub list.
    - RESEARCH.md §1.1 (server tsconfig), §1.3 (Hono), §1.4 (npm scripts), §7.2 (test approach).
  </read_first>
  <action>
    Install runtime deps: `hono`, `@hono/node-server`, `better-sqlite3`. Install devDep `tsx` and `@types/better-sqlite3`. Add four scripts to package.json: `server` = `tsx server/index.ts`, `server:dev` = `tsx watch server/index.ts`, `server:check` = `tsc --noEmit -p server/tsconfig.json`, `server:backup` = `tsx server/db/backup.ts`. Do NOT modify the existing `test` script in this task (the eslint guard task 01-03-01 wires server:check into CI later); leave `test` as-is for now.

    Create `server/tsconfig.json` extending the compiler options but overriding: `"lib": ["ESNext"]` (NO DOM, NO DOM.Iterable), `"types": ["node"]`, `"module": "ESNext"`, `"moduleResolution": "bundler"`, `"noEmit": true`, `"strict": false`, `"strictNullChecks": true`, `"include": ["server"]`, `"baseUrl": "."`. This keeps `tsc --noEmit -p server/tsconfig.json` from type-checking server files with DOM libs (prevents accidental window/document use server-side, per RESEARCH §1.1).

    Add the pragma `// @vitest-environment node` as the FIRST line of every server test stub (server/db/*.test.ts, server/routes/health.test.ts, server/auth/*.test.ts) so they run in Node, not jsdom (VALIDATION Wave 0 line 66 — the simpler pragma approach over a vite.config project entry). The SPA test stubs (src/hooks/useEmailEnabled.test.ts, src/components/admin/AdminLayout.test.tsx) stay in the default jsdom env (no pragma).

    Stub each test file with a `describe` block and `it.todo`/skipped cases matching the VALIDATION Per-Task Verification Map behaviors, so `npx vitest run <file>` succeeds (no failures) before implementation:
    - `server/db/sqlite.repository.test.ts`: cases for CRUD per entity, `UNIQUE(site_id, email)` enforcement, cross-site site_id isolation (T-01-01), status transitions, token invalidate-after-use.
    - `server/db/migrations.test.ts`: idempotency (run twice → second no-op), ordering, schema_migrations recording.
    - `server/db/sqlite.test.ts`: `PRAGMA journal_mode` returns `wal` after open.
    - `server/db/backup.test.ts`: backup produces a valid openable DB with same row count; backup-during-write.
    - `server/routes/health.test.ts`: `GET /api/email/health` → 200 with body `{"ok":true}`, no auth, no DB details.
    - `server/auth/nip98.test.ts`: valid/missing/bad-sig/non-master/expired/wrong-url/wrong-method/payload-tamper/proxy-reconstruction.
    - `server/auth/master-pubkey.test.ts`: env wins, nostr.json fallback, fetch-fail-closed.
    - `src/hooks/useEmailEnabled.test.ts`: env/meta priority, default false, string coercion.
    - `src/components/admin/AdminLayout.test.tsx`: navigation array gates email nav entry on useEmailEnabled (no email nav when false).
    - `eslint-rules/__tests__/guard.test.ts`: src/ importing resend/better-sqlite3/csv-parse/pg or server/* → lint error; server/ importing same → no error (RuleTester/Linter approach, per VALIDATION's RuleTester option).

    Each stub must import from the module path it will test (e.g. `import { SqliteSubscriberRepository } from './sqlite'`) so the import resolves once the implementation lands; use `describe`/`it.todo` so no assertion runs yet. Confirm `@nostrify/nostrify` exports `NIP98` (already verified: `node_modules/@nostrify/nostrify/mod.ts` line 7 re-exports `NIP98` from `./NIP98.ts`; `NIP98.verify(request, opts?)` exists). No code implementation in this task — stubs only.
  </action>
  <acceptance_criteria>
    - `hono`, `@hono/node-server`, `better-sqlite3` present in package.json dependencies; `tsx` and `@types/better-sqlite3` in devDependencies.
    - package.json scripts include `server`, `server:dev`, `server:check`, `server:backup`.
    - `server/tsconfig.json` exists with `"lib": ["ESNext"]` and no `DOM` entry, `"types": ["node"]`, `"include": ["server"]`.
    - All 10 stubbed test files exist; each server test file's first line is the Node-environment pragma.
    - `npx vitest run server/db/sqlite.repository.test.ts server/db/migrations.test.ts server/db/sqlite.test.ts server/db/backup.test.ts server/routes/health.test.ts server/auth/nip98.test.ts server/auth/master-pubkey.test.ts src/hooks/useEmailEnabled.test.ts src/components/admin/AdminLayout.test.tsx eslint-rules/__tests__/guard.test.ts` exits 0 (all stubs pass with no real assertions).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run server/db/sqlite.repository.test.ts server/db/migrations.test.ts server/db/sqlite.test.ts server/db/backup.test.ts server/routes/health.test.ts server/auth/nip98.test.ts server/auth/master-pubkey.test.ts src/hooks/useEmailEnabled.test.ts src/components/admin/AdminLayout.test.tsx eslint-rules/__tests__/guard.test.ts</automated>
  </verify>
</task>

<task id="01-01-01" type="auto" wave="1">
  <objective>SubscriberRepository interface + SQLite implementation with site_id-scoped CRUD (T-01-01 cross-site isolation).</objective>
  <files>server/db/repository.ts, server/db/sqlite.ts</files>
  <read_first>
    - `server/db/sqlite.repository.test.ts` (the stub from 01-01-00 — implement to make its cases pass).
    - RESEARCH.md §2.1 (interface design — the full method list), §2.2 (SQLite impl + WAL + busy_timeout), §2.4 (backend selection).
    - VALIDATION.md task 01-01-01 row (site_id isolation secure behavior).
  </read_first>
  <action>
    Create `server/db/repository.ts` defining the row types and the `SubscriberRepository` interface exactly as specified in RESEARCH §2.1: `Subscriber` (id, site_id, email, name, npub, status union 'pending'|'active'|'unsubscribed'|'bounced', segment string[], created_at, confirmed_at, bounced_at, complained_at), `Settings` (site_id pk, module_enabled, resend_api_key_enc, sending_domain, from_name, postal_address, rate_limit, updated_at), `VerifyToken` (id, subscriber_id, site_id, purpose, expires_at, used), `SendLog` (id, site_id, post_event_id, subject, recipient_count, sent_count, status, started_at, completed_at), `DeliveryEvent` (id, site_id, send_log_id, subscriber_id, event_type, recipient, timestamp). The `SubscriberRepository` interface methods (all returning Promise): getSubscriber(siteId,id), getSubscriberByEmail(siteId,email), listSubscribers(siteId, opts?), countSubscribers(siteId, opts?), insertSubscriber(siteId, sub), updateSubscriberStatus(siteId,id,status), deleteSubscriber(siteId,id), getSettings(siteId), upsertSettings(siteId, settings), createToken(token), getToken(id), invalidateToken(id), createSendLog(entry), updateSendLog(id, patch), findSendLogByPostEventId(siteId, postEventId), recordDeliveryEvent(ev), close(). Every method is scoped by siteId where a site is involved.

    Create `server/db/sqlite.ts` exporting `SqliteSubscriberRepository implements SubscriberRepository`. Constructor takes a dbPath (string). Open a single `better-sqlite3` `Database` instance and reuse it (single-connection pattern, RESEARCH §2.2 — required for safe online backup). On open run `db.pragma('journal_mode = WAL')`, `db.pragma('foreign_keys = ON')`, `db.pragma('busy_timeout = 5000')` (RESEARCH §7.3 edge case). Wrap synchronous better-sqlite3 calls in `Promise.resolve(...)` so the interface is uniformly async (RESEARCH §2.1). The `segment` array is stored as JSON text and parsed/serialized on read/write. `insertSubscriber` generates `id` as `crypto.randomUUID()` and sets `created_at` to `new Date().toISOString()`. Every SELECT/UPDATE/DELETE includes `AND site_id = ?` keyed on the passed siteId — this is the T-01-01 mitigation; the test asserts a row inserted under site_id 'A' is not returned by a lookup under site_id 'B'. `updateSubscriberStatus` transitions status; `invalidateToken` sets `used = 1`. Implement `close()` to close the db handle.

    Use `EMAIL_DB_PATH` env var with default `./email.db` (dev-friendly; installer sets `EMAIL_DB_PATH=/app/email.db` in prod, per RESEARCH §2.2 and CONTEXT.md deferred defaults). Do NOT create the migrations or run them here — that is task 01-01-02; this task only implements the repository class. The repository constructor may assume migrations have been run by the caller (server/index.ts).
  </action>
  <acceptance_criteria>
    - `server/db/repository.ts` exports `SubscriberRepository` interface and the 5 row types; every subscriber/settings/token/sendlog method accepts `siteId` (or token id for getToken/invalidateToken).
    - `server/db/sqlite.ts` exports `SqliteSubscriberRepository` implementing all interface methods; opens a single better-sqlite3 connection with WAL, foreign_keys, busy_timeout pragmas.
    - Cross-site isolation: inserting a subscriber under site_id 'A' and looking it up under site_id 'B' returns null (T-01-01).
    - `npx vitest run server/db/sqlite.repository.test.ts` is green (all stubbed cases now real and passing).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run server/db/sqlite.repository.test.ts</automated>
  </verify>
</task>

<task id="01-01-02" type="auto" wave="1">
  <objective>Idempotent migration runner + initial SQLite schema (5 tables + schema_migrations, all carrying site_id).</objective>
  <files>server/db/migrate.ts, server/db/migrations/001.sqlite.sql</files>
  <read_first>
    - `server/db/migrations.test.ts` (stub from 01-01-00).
    - RESEARCH.md §2.2 (migrations: raw SQL + tiny runner; layout NNN.<dialect>.sql; schema_migrations table; transactional apply).
    - `server/db/sqlite.ts` (from 01-01-01 — the runner takes a better-sqlite3 Database).
  </read_first>
  <action>
    Create `server/db/migrations/001.sqlite.sql` with CREATE TABLE statements for: `subscribers` (id TEXT PRIMARY KEY, site_id TEXT NOT NULL, email TEXT NOT NULL, name TEXT, npub TEXT, status TEXT NOT NULL DEFAULT 'pending', segment TEXT, created_at TEXT NOT NULL, confirmed_at TEXT, bounced_at TEXT, complained_at TEXT, UNIQUE(site_id, email)), `settings` (site_id TEXT PRIMARY KEY, module_enabled INTEGER NOT NULL DEFAULT 0, resend_api_key_enc TEXT, sending_domain TEXT, from_name TEXT, postal_address TEXT, rate_limit INTEGER, updated_at TEXT NOT NULL), `verify_tokens` (id TEXT PRIMARY KEY, subscriber_id TEXT NOT NULL, site_id TEXT NOT NULL, purpose TEXT NOT NULL, expires_at TEXT NOT NULL, used INTEGER NOT NULL DEFAULT 0, FOREIGN KEY(subscriber_id) REFERENCES subscribers(id) ON DELETE CASCADE), `send_log` (id TEXT PRIMARY KEY, site_id TEXT NOT NULL, post_event_id TEXT, subject TEXT, recipient_count INTEGER, sent_count INTEGER, status TEXT, started_at TEXT NOT NULL, completed_at TEXT), `delivery_events` (id TEXT PRIMARY KEY, site_id TEXT NOT NULL, send_log_id TEXT, subscriber_id TEXT, event_type TEXT NOT NULL, recipient TEXT NOT NULL, timestamp TEXT NOT NULL), and `schema_migrations` (version TEXT PRIMARY KEY, applied_at TEXT NOT NULL). Add a partial unique index for send_log dedup: `CREATE UNIQUE INDEX send_log_post_event_id_unique ON send_log(site_id, post_event_id) WHERE post_event_id IS NOT NULL` (Pitfall P10 dedup, per RESEARCH §2.2). Every table includes `site_id` (multi-tenant-ready, per locked decision).

    Create `server/db/migrate.ts` exporting `runMigrations(db: Database, migrationsDir: string): Promise<void>`. The runner: ensures `schema_migrations` table exists; reads files matching `/^(\d+)\.sqlite\.sql$/` in the directory; sorts by the numeric prefix; for each version not already in `schema_migrations`, executes the SQL inside a `db.transaction(() => { exec(sql); insert(version, applied_at) })()` and records the version with `new Date().toISOString()`. Idempotent: re-running skips already-applied versions (the test runs it twice and asserts no error + schema_migrations row count == file count). Throw a clear error if a migration file is missing or SQL fails. The runner is dialect-specific to SQLite (Postgres dialect is additive later, RESEARCH §2.2).
  </action>
  <acceptance_criteria>
    - `server/db/migrations/001.sqlite.sql` creates all 5 entity tables + schema_migrations; every entity table has a `site_id` column; subscribers has `UNIQUE(site_id, email)`.
    - `runMigrations` is idempotent: calling it twice on the same temp DB applies files once; `schema_migrations` has one row per migration file.
    - `npx vitest run server/db/migrations.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run server/db/migrations.test.ts</automated>
  </verify>
</task>

<task id="01-01-03" type="auto" wave="1">
  <objective>WAL mode verification — opening the SQLite repository enables WAL (PRAGMA journal_mode returns wal).</objective>
  <files>server/db/sqlite.test.ts</files>
  <read_first>
    - `server/db/sqlite.ts` (from 01-01-01 — confirms WAL pragma on open).
    - `server/db/sqlite.test.ts` (stub from 01-01-00).
    - RESEARCH.md §2.2 (WAL mode), §7.1 (WAL test: PRAGMA journal_mode returns wal).
  </read_first>
  <action>
    Fill the `server/db/sqlite.test.ts` cases (stubbed in 01-01-00) to assert WAL is enabled after opening `SqliteSubscriberRepository` on a temp file DB (use `os.tmpdir()` + unique filename, or `:memory:` if WAL is observable there — note WAL on `:memory:` is a no-op, so use a temp file path for this assertion). The test opens the repository, queries `db.pragma('journal_mode')` (expose a test-only accessor or query via the raw connection if the class exposes it; if not, add a minimal `pragma(name)` method on the repository for testability), and asserts the result equals `wal`. Also assert `foreign_keys` is ON and `busy_timeout` is 5000. Clean up the temp file in afterEach. If `SqliteSubscriberRepository` does not expose a `pragma` accessor, add a small `pragma(name: string): unknown` method (test-friendly) rather than leaking the raw db handle — implement it in `server/db/sqlite.ts` (edit allowed). Keep WAL enablement in the constructor (already done in 01-01-01); this task only adds the test assertions + any minimal accessor needed.
  </action>
  <acceptance_criteria>
    - After opening `SqliteSubscriberRepository`, `PRAGMA journal_mode` returns `wal`.
    - `PRAGMA foreign_keys` returns 1 (ON); `PRAGMA busy_timeout` returns 5000.
    - `npx vitest run server/db/sqlite.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run server/db/sqlite.test.ts</automated>
  </verify>
</task>

<task id="01-01-04" type="auto" wave="1">
  <objective>Online backup capability via better-sqlite3 backup API + documented daily backup path (SRV-02 "online-backup path is documented").</objective>
  <files>server/db/backup.ts, server/README.md</files>
  <read_first>
    - `server/db/backup.test.ts` (stub from 01-01-00).
    - RESEARCH.md §2.3 (online backup: db.backup(destination) returns a Promise; single-connection pattern; backup during write; recommended defaults — EMAIL_DB_PATH default /app/email.db, daily backup to EMAIL_BACKUP_DIR/email.db.bak default /app/backups, 7-day retention).
    - `server/db/sqlite.ts` (the repository holds the single connection used for backup).
  </read_first>
  <action>
    Create `server/db/backup.ts` exporting `backupDatabase(db: Database, destPath: string): Promise<void>` that calls `db.backup(destPath)` (better-sqlite3's online backup API — returns a Promise, safe to call while the db is in use because only the single connection mutates, RESEARCH §2.3). Also export `runBackup(): Promise<void>` that reads `EMAIL_DB_PATH` (default `/app/email.db`) and `EMAIL_BACKUP_DIR` (default `/app/backups`), opens the source db, calls `backupDatabase` to `${EMAIL_BACKUP_DIR}/email.db.bak`, then rotates backups older than 7 days by mtime (delete files older than 7*24*60*60*1000 ms in the backup dir matching `email.db.bak*`). Ensure the destination directory exists (`fs.mkdirSync(dir, { recursive: true })`).

    Create `server/README.md` documenting: env vars (`EMAIL_PORT` default 3001 per D-01, `EMAIL_DB_PATH` default `./email.db` dev / `/app/email.db` prod, `EMAIL_DB_BACKEND` default `sqlite`, `EMAIL_BACKUP_DIR` default `/app/backups`); dev run (`npm run server` / `npm run server:dev`); prod run (`tsx server/index.ts` or compiled `node dist-server/index.js`); the daily backup cron line: `0 3 * * * cd /app/nostr-cms && npm run server:backup` (or the node -e equivalent); 7-day retention note. This satisfies SRV-02's "online-backup path is documented" requirement.

    Fill `server/db/backup.test.ts` cases: (1) backup a temp db with N rows → the backup file opens and has the same row count; (2) start a backup, insert a row mid-backup, assert the backup completes without error and contains a valid db (the inserted row may or may not be in the snapshot — assert the file is openable and consistent, not a specific count, per RESEARCH §7.3). Clean up temp files in afterEach.
  </action>
  <acceptance_criteria>
    - `backupDatabase(db, destPath)` produces a valid, openable SQLite file with the same row count as the source when no concurrent write occurs.
    - A backup started while a write is in progress completes without error and yields an openable db.
    - `runBackup()` respects `EMAIL_DB_PATH` and `EMAIL_BACKUP_DIR` env vars and rotates backups older than 7 days.
    - `server/README.md` documents the env vars, run commands, and the daily backup cron line.
    - `npx vitest run server/db/backup.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run server/db/backup.test.ts</automated>
  </verify>
</task>

<task id="01-01-05" type="auto" wave="1">
  <objective>Server entry point + public health route (SRV-01 scaffold root + D-01 port + D-04 health). The health route's integration test is owned by task 01-02-05 — this task only creates the route + entry + a testable `createApp` helper; it does NOT fill `server/routes/health.test.ts`.</objective>
  <files>server/index.ts, server/routes/health.ts</files>
  <read_first>
    - `server/routes/health.test.ts` (stub from 01-01-00 — read only; do NOT fill it here, task 01-02-05 owns the test body).
    - RESEARCH.md §1.3 (Hono on @hono/node-server), §1.4 (npm run server entry), §4.1 last paragraph (health endpoint D-04 — public, no auth, returns {"ok":true}, registered before the nip98AdminAuth middleware).
    - `server/db/sqlite.ts`, `server/db/migrate.ts` (from 01-01-01/02 — the entry opens the repo and runs migrations).
    - CONTEXT.md D-01 (EMAIL_PORT default 3001), D-04 (health public, {"ok":true} only).
  </read_first>
  <action>
    Create `server/routes/health.ts` exporting a Hono handler (e.g. `export const healthRoute = (c: Context) => c.json({ ok: true }, 200)`). The response body is exactly the object with the ok key set true — no DB details, no subscriber counts, no config (D-04). This route is registered BEFORE any auth middleware in the app so it is public.

    Create `server/index.ts` as the single server entry. It: reads `EMAIL_PORT` (default `3001`, per D-01) and `EMAIL_DB_BACKEND` (default `sqlite`); constructs a `Hono` app; when backend is sqlite, opens `new SqliteSubscriberRepository(process.env.EMAIL_DB_PATH || './email.db')`, gets its underlying better-sqlite3 Database (expose a way — e.g. the repo's `pragma` accessor from 01-01-03 implies a handle; add a `getDb()` or have the runner accept the repo; simplest: open the better-sqlite3 Database in index.ts, run `runMigrations(db, migrationsDir)`, then construct `SqliteSubscriberRepository` with that db — adjust the repository constructor to accept either a path or a Database; prefer accepting a Database so index.ts owns the single connection and can run migrations on it before handing it to the repo), registers `app.get('/api/email/health', healthRoute)`, and starts `serve({ fetch: app.fetch, port: Number(EMAIL_PORT) })` from `@hono/node-server`, logging the port. Leave a clear extension point (e.g. a `registerRoutes(app, repo)` call or inline registration) so plan 01-02 can add the admin auth middleware + admin routes without rewriting the entry. Do NOT add admin routes or auth here — that is plan 01-02.

    Factor the Hono app construction into a testable `export function createApp(repo?: SubscriberRepository): Hono` helper inside `server/index.ts` (or a small `server/app.ts` if you prefer — but keep it imported by `server/index.ts` so there is one app source). `createApp` registers the public health route and returns the Hono instance WITHOUT binding a port; the `serve()` call stays in the `if (require.main === module)`-style entry path so importing `createApp` in a test does not start listening. This helper exists so task 01-02-05 can mount the app in `server/routes/health.test.ts` via `createApp()` without binding a port. Do NOT fill the test file in this task — 01-02-05 owns the test body.
  </action>
  <acceptance_criteria>
    - `server/index.ts` reads `EMAIL_PORT` (default 3001, D-01) and starts @hono/node-server.
    - `server/index.ts` exports a `createApp` function that registers the public health route and returns a Hono instance without binding a port.
    - `server/routes/health.ts` exports a handler returning exactly `{ ok: true }` with no DB/config/subscriber-count fields (D-04).
    - The health route is registered without auth (public), before any admin middleware.
    - `server/routes/health.test.ts` is left as the Wave 0 stub (not filled by this task; 01-02-05 fills it).
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^\s*//' server/index.ts | grep -c 'createApp' && grep -v '^\s*//' server/routes/health.ts | grep -c 'healthRoute' && npx tsc --noEmit -p server/tsconfig.json</automated>
  </verify>
</task>

</tasks>
